{% extends 'app_layout.html.twig' %}

{% block title %}Change Password - FileShareZ{% endblock %}

{% block content %}
<div class="max-w-2xl mx-auto space-y-6 sm:space-y-7">
    <div class="animate-in">
        <a href="{{ path('app_account') }}" class="text-aero/60 hover:text-aero text-xs uppercase tracking-wider">&larr; Back to Account</a>
        <h1 class="text-xl sm:text-2xl font-light tracking-wide text-white/90 mt-2">Change Password</h1>
        <p class="text-white/35 mt-1 text-sm">Update your account password</p>
    </div>
    <div class="glass animate-in p-6 sm:p-8" style="animation-delay: 0.1s">
        {{ form_start(form) }}
            <div class="space-y-5">
                <div>
                    {{ form_label(form.currentPassword, null, {'label_attr': {'class': 'block text-xs font-medium text-white/40 mb-1.5 uppercase tracking-wider'}}) }}
                    {{ form_widget(form.currentPassword) }}
                    {{ form_errors(form.currentPassword) }}
                </div>
                <div>
                    {{ form_label(form.newPassword.first, null, {'label_attr': {'class': 'block text-xs font-medium text-white/40 mb-1.5 uppercase tracking-wider'}}) }}
                    {{ form_widget(form.newPassword.first) }}
                    {{ form_errors(form.newPassword.first) }}
                </div>
                <div>
                    {{ form_label(form.newPassword.second, null, {'label_attr': {'class': 'block text-xs font-medium text-white/40 mb-1.5 uppercase tracking-wider'}}) }}
                    {{ form_widget(form.newPassword.second) }}
                    {{ form_errors(form.newPassword.second) }}
                </div>
                <button type="submit" class="aero-btn-primary px-8 py-2.5 text-sm">Change Password</button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}